import Header from "./Header/Header.jsx";
import Chat from "./Chat/Chat.jsx";

const Home = ({user}) => {
    return (
        <>
            <Header user={user}/>
        </>

    );
};

export default Home;
